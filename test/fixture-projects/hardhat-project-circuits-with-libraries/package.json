{
  "name": "hardhat-project-circuits-with-libraries",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile --force",
    "clean": "hardhat clean && rm -rf artifacts && rm -rf cache"
  },
  "dependencies": {
    "circomlib": "^2.0.5"
  }
}
